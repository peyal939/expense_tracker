# Copy to .env and adjust for local dev
# NOTE: Django's SECRET_KEY is currently the default generated one in settings.py.
# For production, always set a strong SECRET_KEY and set DEBUG=False.

# Allowed hosts (comma-separated), e.g. localhost,127.0.0.1
# ALLOWED_HOSTS=localhost,127.0.0.1

# If you later want stricter CORS, set these and switch CORS_ALLOW_ALL_ORIGINS to False
# CORS_ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# AI (future)
# AI_ENABLED=false
# OLLAMA_BASE_URL=http://localhost:11434
# OLLAMA_MODEL=llama3
# OLLAMA_TIMEOUT_SECONDS=10
